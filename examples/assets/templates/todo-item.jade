li(class="#{completed}", data-model-id="#{id}")
  .view
    if(completed)
      input.toggle(type="checkbox", checked)
    else
      input.toggle(type="checkbox")
    label= title
    button.destroy
  input.edit(value="#{title}")